{% extends "base.html.twig" %}

{% block content %}

    <h2>Session</h2>
    <br>
    <div class="row">
        <div class="mb-3 col-md-auto">
            <a class="btn btn-primary" href="{{ path("session_supprimer", {"id":session.id}) }}">Supprimer</a>
        </div>
        <div class="mb-3 col-md-auto">
            <a class="btn btn-secondary" href="{{ path("calcul_score_form", {"session_id":session.id}) }}">Calculer
                les
                scores</a>
        </div>
        <div class="mb-3 col-md-auto">
            <a class="btn btn-secondary" href="{{ path("calcul_profil_session_form", {"session_id":session.id}) }}">Calculer
                les
                profils</a>
        </div>
        <div class="mb-3 col-md-auto">
            <a class="btn btn-secondary" href="{{ path("session_csv", {"id":session.id}) }}">Exporter en .csv</a>
        </div>
        <div class="mb-3 col-md-auto">
            <a class="btn btn-secondary" href="{{ path("lecture_optique", {"id":session.id}) }}">Lire des feuilles</a>
        </div>
        <div class="mb-3 col-md-auto">
            <a class="btn btn-secondary" href="{{ path("session_index") }}">Retour à la liste</a>
        </div>
    </div>
    <br>
    <hr class="hr hr-blurry">
    <br>
    <h3>Informations générales</h3>
    <br>
    <dl class="row">
        <dt class="col-md-3">Date</dt>
        <dd class="col-md-9">{{ session.date|date('d-m-Y') }}</dd>
        <dt class="col-md-3">SGAP</dt>
        <dd class="col-md-9">{{ session.sgap.nom }}</dd>
        <dt class="col-md-3">Concours</dt>
        <dd class="col-md-9">{{ session.concours.nom }}</dd>
        <dt class="col-md-3">Version batterie</dt>
        <dd class="col-md-9">{{ session.concours.version_batterie }}</dd>
        <dt class="col-md-3">Type de concours</dt>
        <dd class="col-md-9">{{ session.concours.type_concours }}</dd>
    </dl>
    <br>
    <hr class="hr hr-blurry">
    <br>
    <h3>Réponses des candidats</h3>
    <br>
    <table class="table d-md-inline-block overflow-auto">
        <thead>
        <tr class="text-nowrap">
            <th scope="col">Nom</th>
            <th scope="col">Prenom</th>
            <th scope="col">Nom de jeune fille</th>
            <th scope="col">Date de naissance</th>
            <th scope="col">Sexe</th>
            <th scope="col">Niveau scolaire</th>
            <th scope="col">Reponses brutes</th>
            <th scope="col">Supprimer</th>
        </tr>
        </thead>
        <tbody>
        {% for reponse in session.reponses_candidats %}
            <tr>
                <td>{{ reponse.nom }}</td>
                <td>{{ reponse.prenom }}</td>
                <td>{{ reponse.nom_jeune_fille }}</td>
                <td>{{ reponse.date_de_naissance|date("d/m/Y") }}</td>
                <td>{{ reponse.sexe|sexe }}</td>
                <td>{{ reponse.niveau_scolaire.nom }}</td>
                <td class="text-truncate">{{ reponse.reponses|join("-") }}</td>
                <td><a class="btn btn-primary"
                       href="{{ path("reponse_candidat_supprimer", {"id":reponse.id}) }}">Supprimer</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}

