{% extends 'base.html.twig' %}

{% block content %}
    <br>
    <h1>Concours</h1>
    <br>
    <div class="note note-info">
        Un concours est le regroupement d'un <b>intitulé</b> et d'un <b>type concours</b>.
    </div>
    <br>
    <div class="note note-info">
        Les <b><a href="{{ path("test_index") }}">tests</a></b> peuvent s'appliquer à certains concours et pas à
        d'autres.
    </div>
    <br>
    <div class="row row-cols-auto">
        <div class="col">
            <a class="btn btn-primary" href="{{ path("concours_creer") }}">Créer un concours</a>
        </div>
    </div>
    <br>
    <table class="table">
        <thead>
        <tr>
            <th>Nom</th>
            <th>Type de concours</th>
            <th>Modifier</th>
            <th>Supprimer</th>
        </tr>
        </thead>
        <tbody>
        {% for concours in concours %}
            <tr class="align-middle">
                <td>{{ concours.intitule }}</td>
                <td>{{ concours.type_concours }}</td>
                <td>
                    <a class="btn btn-link fw-bold"
                       href="{{ path("concours_modifier", {"id":concours.id}) }}">Lien</a>
                </td>
                <td>
                    <a class="btn btn-link fw-bold link-danger"
                       href="{{ path("concours_supprimer_confirmer", {"id":concours.id}) }}">
                        <i class="fa fa-check"></i>
                        Lien
                    </a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <br>
{% endblock %}

