{% extends 'base.html.twig' %}

{% block content %}
    <br>
    <h1>Utilisateurs</h1>
    <br>
    <p class="lead">Liste des utilisateurs ayant accès à l'application Cortest.</p>
    <br>
    <div class="row row-cols-auto">
        <div class="col">
            <a class="btn btn-primary" href="{{ path("admin_creer") }}">Créer</a>
        </div>
    </div>
    <br>
    <table class="table">
        <thead>
        <tr>
            <th>Nom d'utilisateur</th>
            <th>Rôle</th>
            <th>Modifier</th>
            <th>Supprimer</th>
        </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.username }}</td>
                <td>{{ user.role }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path("admin_modifier", {"id":user.id}) }}">Modifier</a>
                </td>
                <td>
                    <a class="btn btn-primary" href="{{ path("admin_supprimer", {"id":user.id}) }}">Supprimer</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <br>
{% endblock %}

